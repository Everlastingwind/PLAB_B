# PlanB 项目全局业务规则 (AI 必须严格遵守)

## 规则 1：比赛数据源分类与隔离 (Pub vs Pro)
- **本地数据 (Pub)**：通过本地解析器（如 `parser_fixed.go`、`import_local.py` 等）处理上传的录像，必须在数据中强行注入字段 `"match_tier": "pub"` 或 `"match_source": "local"`。
- **隔离墙**：只要一场比赛被标记为本地/pub，**绝对禁止**前端或后端再去 OpenDota API 发起任何数据补全请求。所有数据（包括装备、英雄、击杀等）仅从本地数据库/JSON读取。
- **云端数据 (Pro)**：直接通过 OpenDota API 拉取的职业比赛数据，必须打上 `"match_tier": "pro"` 标签。

## 规则 2：玩家 ID 匿名脱敏化
- 无论是入库还是前端渲染，必须对玩家名字进行严格判定。
- 如果该玩家有职业认证（存在有效的 `pro_name`、`is_pro` 或 `account_id` 匹配到了职业库），则显示其真实职业 ID。
- 如果是普通玩家（无职业认证），必须将其名字强制替换或渲染为 **"匿名"**（不要暴露原本的 `personaname` 或 Steam 昵称）。
